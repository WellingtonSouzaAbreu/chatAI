import { KeyValueScheduling } from "./types"

const defaultScheduleKeys: KeyValueScheduling[] = [
    { date: 'data' },
    { hour: 'hora' },
    { service: 'serviço do salão' }
]

export { defaultScheduleKeys }